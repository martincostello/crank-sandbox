components:
  sandbox:
    script: |
      call ./build.ps1
      call dotnet build-server shutdown
    arguments: ""

defaults: --config ./crank-pr.yml

profiles:
  local:
    description: Local
    arguments: --profile local

benchmarks:
  root:
    description: Root
    arguments: --config ./crank-pr.yml --scenario root
